interface:
  display_name: "Agent Config Review"
  short_description: "Run the Agent Config Review workflow"
  default_prompt: "Use $agent-config-review to apply this workflow to the current repository."
