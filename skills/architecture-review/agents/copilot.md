When asked to review architecture: read all module directories and build config files (build.gradle, package.json, pyproject.toml, Cargo.toml, go.mod) to map module boundaries, identify the declared architecture pattern from README or comments, trace import graphs in each layer and verify dependency direction flows strictly presentation → domain → data with no reversals, flag any circular dependencies between modules (A imports B, B imports C, C imports A), identify god modules with more than 3 unrelated responsibilities or that are imported by every other module, check DI wiring files for bindings placed in the wrong layer, flag tight coupling where swapping one module forces changes in unrelated modules, flag low cohesion where a module mixes concerns that belong in separate units, detect layer violations such as UI logic in data layer or database queries in views, then output all findings ordered P0 (circular deps, domain importing infrastructure) through P3 (naming inconsistencies, minor structural smells) with file path and line reference for each finding.
