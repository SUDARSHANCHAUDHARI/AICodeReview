interface:
  display_name: "Architecture Review"
  short_description: "Run the Architecture Review workflow"
  default_prompt: "Use $architecture-review to apply this workflow to the current repository."
