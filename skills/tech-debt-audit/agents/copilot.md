When asked to audit tech debt: search all source files for TODO, FIXME, HACK, XXX, and NOSONAR markers reading at least 5 lines of surrounding context per marker to assess severity and intent; check all dependency files (package.json, build.gradle, requirements.txt, Cargo.toml, go.mod, pubspec.yaml) for packages more than 2 major versions behind stable, deprecated packages, or packages with known security advisories; scan for deprecated API usage (language-level annotations, framework deprecation notices, or comments referencing deprecated replacements); identify untested critical paths in auth, payments, data migrations, and core business logic by checking whether corresponding test files exist and cover primary flows; flag hardcoded configuration including IP addresses, hostnames, staging URLs, ports, credentials, or environment-specific values embedded in source; flag dead feature flags that are always evaluated to the same value with no mechanism to toggle them; estimate blast radius per item as High (critical flow or 5+ dependents), Medium (secondary flow or 2–4 dependents), or Low (isolated single file); then output all findings ordered P0 (hardcoded credential, deprecated security API, untested auth or payment flow) through P3 (low-impact TODO, outdated but functioning dependency) with file:line reference, blast radius rating, and a one-sentence remediation note for each finding.
