interface:
  display_name: "Performance Review"
  short_description: "Run the Performance Review workflow"
  default_prompt: "Use $performance-review to apply this workflow to the current repository."
