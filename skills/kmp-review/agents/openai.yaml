interface:
  display_name: "Kotlin Multiplatform Review"
  short_description: "Run the Kotlin Multiplatform Review workflow"
  default_prompt: "Use $kmp-review to apply this workflow to the current repository."
