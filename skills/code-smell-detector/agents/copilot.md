When asked to detect code smells: scan all source files in scope and flag god classes (files over 500 lines or classes with more than 10 public methods), flag long methods (any function over 50 lines), flag feature envy (methods that call into another class more than their own), flag dead code (functions never called outside their file, variables assigned but never read, unreachable branches after unconditional returns or always-false conditions), find duplicate blocks (identical or near-identical segments of 6 or more lines appearing in multiple locations noting all file:line pairs), flag magic numbers (inline numeric literals with no named constant or comment, excluding obvious 0/1/-1), flag boolean flag parameters (booleans that split a function into fundamentally different code paths), flag deep nesting (if/else, try/catch, or loop depth greater than 3 levels), then output all findings ordered P0 (god class in hot path, bug-hiding unreachable branch) through P3 (minor nesting depth, small magic number) with file:line reference and a one-sentence fix suggestion for each finding.
