interface:
  display_name: "Code Smell Detector"
  short_description: "Run the Code Smell Detector workflow"
  default_prompt: "Use $code-smell-detector to apply this workflow to the current repository."
