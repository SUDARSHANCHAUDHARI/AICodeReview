interface:
  display_name: "Accessibility Audit"
  short_description: "Run the Accessibility Audit workflow"
  default_prompt: "Use $accessibility-audit to apply this workflow to the current repository."
