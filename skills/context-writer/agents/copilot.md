## Context Writer

When asked to create or update `PROJECT_CONTEXT.md`: read actual stack files, entry points, source, and tests — do not guess. Discover real commands from package scripts, Makefiles, or CI. Fill all relevant sections with verified facts only. Never include secrets, tokens, signing material, or customer data.
