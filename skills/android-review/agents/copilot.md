## Android Review

When asked to review Android/Kotlin/KMP changes: check Compose state hoisting and recomposition safety, ViewModel exposing `StateFlow<UiState>`, coroutine dispatcher correctness, Room off-main-thread, Hilt scoping, typed navigation arguments, accessibility labels, no committed secrets or signing material, and AAB-oriented release config. Order findings by severity with `file.kt:line` references.
