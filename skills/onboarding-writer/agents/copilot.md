When asked to generate onboarding documentation: read the repository root listing, the full existing README, all build config files (package.json, build.gradle, settings.gradle, pyproject.toml, setup.py, Cargo.toml, go.mod, Makefile, Dockerfile, docker-compose.yml), all CI config files (.github/workflows/*.yml, .gitlab-ci.yml, .circleci/config.yml, Jenkinsfile), the application entry point file, the DI or service wiring file if present, the environment config example (.env.example, config.example.yml), and any existing architecture docs; then write ONBOARDING.md with six sections sourced only from what you read: Project Purpose (what it does, who uses it, why), Architecture Overview (detected pattern, layers, directory tree with one-line descriptions per folder), Local Setup (numbered steps covering prerequisites, environment variable names from .env.example, install commands, and the run command), Key Flows to Understand First (3–5 critical flows with entry point file:function and layer trace), Common Pitfalls (gotchas from README warnings or code comments only, nothing invented), and Domain Glossary (domain terms found in code or docs with one-line definitions); for any section where the repository provides insufficient information write "Not enough information found in the repository to complete this section"; if an ONBOARDING.md already exists read it first and update rather than overwrite it.
