interface:
  display_name: "Codebase Explainer"
  short_description: "Run the Codebase Explainer workflow"
  default_prompt: "Use $codebase-explainer to apply this workflow to the current repository."
