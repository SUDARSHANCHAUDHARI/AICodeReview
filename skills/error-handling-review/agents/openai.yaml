interface:
  display_name: "Error Handling Review"
  short_description: "Run the Error Handling Review workflow"
  default_prompt: "Use $error-handling-review to apply this workflow to the current repository."
