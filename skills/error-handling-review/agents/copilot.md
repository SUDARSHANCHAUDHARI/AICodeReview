When asked to review error handling: read all source files in scope and flag silent catch blocks (catch/except/rescue with no log statement, no rethrow, and no meaningful action), flag inconsistent error propagation (same error category handled with exceptions in some call sites and result types or error codes in others), flag missing UI error states (screens or components that make network or DB calls but show no error branch to the user), flag raw error exposure to users (exception text, stack traces, or internal codes surfaced in user-facing messages), flag retry logic without backoff (loops with no delay, no cap on retries, or retrying non-retryable status codes such as 400 or 401), flag unhandled boundaries at network calls, DB writes, and filesystem access, flag untyped errors propagated as raw strings or untyped objects, flag public APIs that can fail without declared error contracts, then output all findings ordered P0 (silent catch swallowing data-integrity or security errors, unhandled DB write boundary) through P3 (minor error naming inconsistency, missing doc on internal API) with file:line reference and a one-sentence fix suggestion for each finding.
