interface:
  display_name: "React Native Review"
  short_description: "Run the React Native Review workflow"
  default_prompt: "Use $react-native-review to apply this workflow to the current repository."
