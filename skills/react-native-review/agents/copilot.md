When asked to review React Native code: read all source files and flag JS thread blocking (synchronous heavy computation such as crypto, parsing, or large sorts running on the JS thread without a worker or native module offload), flag bridge calls made inside tight loops or rapid event handlers without batching, flag lists of more than 20 items rendered via ScrollView instead of FlatList or SectionList, flag missing useCallback and useMemo on event handlers and callbacks passed to native components, flag memory leaks including event listeners, DeviceEventEmitter subscriptions, timers, and store subscriptions not removed in useEffect cleanup or componentWillUnmount, flag OTA update mechanisms (CodePush) missing staged rollout configuration or rollback setup, flag business logic tangled with inline Platform.OS checks instead of isolated in platform-specific files, flag deep link URL parameters used directly in navigation or API calls without sanitisation, flag navigation patterns with unpredictable back-stack mutations, then output all findings ordered P0 (JS blocking on main interaction path, unsanitised deep link params, critical screen memory leak) through P3 (minor memoization opportunity, CodePush staged rollout not configured) with file:line reference and a one-sentence fix for each finding.
